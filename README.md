# vs_code_projects_public

Public projects, one folder each.

| Project | What it is |
| --- | --- |
| [md_to_html_viewer](md_to_html_viewer/) | A single HTML file that renders Markdown in your browser. No install, no web server, no build step. Drop a `.md` on it. |

Each project is self-contained and carries its own README and license.
